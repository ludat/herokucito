# Stage-specific config (injected by ApplicationSet)
stage:
  name: null                      # stage name (e.g., staging, production)
  project: null                   # Kargo/ArgoCD project namespace
  valuesRepo:
    url: null
    branch: null
    path: null                    # e.g., staging/rendered_values.yaml
  promotion:
    auto: false
  # Additional warehouse subscriptions (e.g., shared infra/chart warehouse)
  warehouses: []
  # warehouses:
  #   - name: infra
  #     direct: true

# ============================================================
# Same structure as herokucito-app values
# ============================================================

name: ""

services: {}
# services:
#   backend:
#     image:
#       repository: myregistry/backend
#       tag: latest
#       source:
#         type: semver                   # "semver", "lexical", "digest", or "stage"
#         constraint: "^1.x"             # for type: semver - semver constraint
#         pattern: "v*"                  # for type: lexical - tag glob pattern
#         tag: "latest"                  # for type: digest - mutable tag to track
#         upstream: staging              # for type: stage - upstream stage name
#     port: 8080
#     resources: small
#     dependencies: [db]
#     routes:
#       - host: myapp.example.com
#         path: /api

dependencies: {}

hooks:
  pre-deploy: []
  post-deploy: []

jobs: {}

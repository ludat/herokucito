apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: "review-{{ .Values.branch }}"
  annotations:
    kargo.akuity.io/color: purple
spec:
  freightCreationPolicy: Automatic
  interval: 5m0s
  subscriptions:
    - image:
        allowTags: ^{{ .Values.branch }}-\d{8}-\d{6}$
        discoveryLimit: 20
        imageSelectionStrategy: Lexical
        repoURL: {{ .Values.imageUrl | quote }}
        strictSemvers: false

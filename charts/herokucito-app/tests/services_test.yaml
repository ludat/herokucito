suite: services
templates:
  - templates/services.yaml
tests:
  - it: renders service for a single service
    set:
      services:
        backend:
          image:
            repository: myregistry/backend
            tag: v1.0.0
          port: 8080
    asserts:
      - matchSnapshot: {}

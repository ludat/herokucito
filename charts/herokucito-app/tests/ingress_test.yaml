suite: ingress
templates:
  - templates/ingress.yaml
tests:
  - it: renders ingress with routes
    set:
      services:
        backend:
          image:
            repository: myregistry/backend
            tag: v1.0.0
          port: 8080
          routes:
            - host: myapp.example.com
              path: /api
    asserts:
      - matchSnapshot: {}
